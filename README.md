# StremioLabAR - دليل المطور

موقع عربي متخصص في إضافات وشروحات تطبيق Stremio. هذا الدليل يوضح كيفية إضافة إضافات جديدة أو شروحات للموقع.

## 🚀 تشغيل المشروع محلياً

**المتطلبات:** Node.js

1. تثبيت المكتبات:
   ```bash
   npm install
   ```

2. تشغيل المشروع:
   ```bash
   npm run dev
   ```

## 📦 إضافة إضافة جديدة

لإضافة إضافة جديدة لتطبيق Stremio، اتبع الخطوات التالية:

### 1. تحديث ملف `App.tsx`

ابحث عن المتغير `addon` في دالة `AddonsView` وقم بتحديث البيانات:

```typescript
const addon = {
  id: 1,
  name: 'اسم الإضافة الجديدة',
  description: 'وصف مختصر للإضافة',
  fullDescription: 'وصف مفصل وشامل عن الإضافة وميزاتها',
  version: '1.0.0',
  imageUrl: 'رابط صورة الإضافة',
  installUrl: 'رابط تثبيت الإضافة',
  tags: ['تصنيف1', 'تصنيف2', 'تصنيف3']
};
```

### 2. إضافة صورة الإضافة

- ضع صورة الإضافة في مجلد `public/images/`
- استخدم صيغة JPG أو PNG
- الحجم المفضل: 400x300 بكسل
- حدث رابط `imageUrl` ليشير للصورة الجديدة

### 3. رابط التثبيت

الرابط سيعمل تلقائياً من خاصية `installUrl` في بيانات الإضافة. لا حاجة لتعديل إضافي!

## 📚 إضافة شرح جديد

لإضافة شرح تعليمي جديد، اتبع الخطوات التالية:

### 1. تحديث ملف `App.tsx`

ابحث عن المتغير `tutorial` في دالة `TutorialsView` وقم بتحديث البيانات:

```typescript
const tutorial = {
  id: 1,
  title: 'عنوان الشرح الجديد',
  description: 'وصف مفصل عن محتوى الشرح وما سيتعلمه المستخدم',
  date: '2024-12-14',
  tutorialUrl: 'رابط الشرح (يوتيوب، تويتر، إلخ)'
};
```

### 2. رابط الشرح

الرابط سيعمل تلقائياً من خاصية `tutorialUrl` في بيانات الشرح. لا حاجة لتعديل إضافي!

### مثال كامل لشرح جديد

```typescript
const tutorial = {
  id: 2,
  title: 'كيفية استخدام Real-Debrid مع Stremio',
  description: 'شرح شامل لربط حساب Real-Debrid مع تطبيق Stremio للحصول على أفضل جودة مشاهدة بدون تقطيع',
  date: '2024-12-15',
  tutorialUrl: 'https://youtube.com/watch?v=example123'
};
```

## 🎨 تخصيص التصميم

### مثال كامل لإضافة جديدة

```typescript
const addon = {
  id: 2,
  name: 'OpenSubtitles',
  description: 'أفضل مصدر للترجمات العربية والأجنبية',
  fullDescription: 'إضافة أساسية لأي مستخدم عربي. توفر ترجمات دقيقة ومتزامنة لمعظم الأفلام والمسلسلات.',
  version: '3.0.0',
  imageUrl: 'https://example.com/opensubtitles-logo.png',
  installUrl: 'https://opensubtitles-addon.com/install',
  tags: ['ترجمة', 'عربي', 'أدوات']
};
```

### الألوان الأساسية

الموقع يستخدم نظام ألوان محدد في ملف CSS:

```css
:root {
  --brand-purple: #6C2BD9;
  --brand-dark: #0a0a0a;
  --brand-card: #111111;
}
```

### إضافة أنيميشن جديد

لإضافة تأثيرات حركية جديدة، أضف الكلاسات في ملف CSS:

```css
.animate-fadeIn {
  animation: fadeIn 0.6s ease-out;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}
```

## 📱 إضافة صفحة جديدة

لإضافة صفحة جديدة للموقع:

### 1. تحديث الأنواع

في ملف `types.ts`، أضف الصفحة الجديدة:

```typescript
export type ViewState = 'home' | 'addons' | 'tutorials' | 'about' | 'new-page';
```

### 2. إضافة المكون الجديد

في ملف `App.tsx`، أضف مكون الصفحة الجديدة:

```typescript
const NewPageView: React.FC = () => {
  return (
    <div className="pt-28 pb-12 px-4 max-w-5xl mx-auto min-h-screen animate-fadeIn">
      <h2 className="text-3xl md:text-4xl font-bold text-white mb-4">عنوان الصفحة</h2>
      {/* محتوى الصفحة */}
    </div>
  );
};
```

### 3. إضافة الصفحة للقائمة

في مصفوفة `navItems` داخل مكون `Navbar`:

```typescript
{ id: 'new-page', label: 'الصفحة الجديدة', icon: <Icon size={18} /> }
```

### 4. إضافة الصفحة للتوجيه

في دالة `renderView`:

```typescript
case 'new-page':
  return <NewPageView />;
```

## 🔧 نصائح التطوير

### استخدام الأيقونات

الموقع يستخدم مكتبة Lucide React للأيقونات:

```typescript
import { IconName } from 'lucide-react';
<IconName size={20} />
```

### التصميم المتجاوب

استخدم كلاسات Tailwind للتصميم المتجاوب:

```typescript
className="text-sm md:text-lg lg:text-xl"
```

### الاتجاه العربي (RTL)

الموقع يدعم الاتجاه العربي تلقائياً. استخدم:
- `mr-4` بدلاً من `ml-4` للهوامش
- `text-right` للنصوص العربية
- `space-x-reverse` للمسافات بين العناصر

## 📋 قائمة المراجعة

قبل نشر التحديثات، تأكد من:

- [ ] اختبار الموقع على الهاتف والكمبيوتر
- [ ] التأكد من عمل جميع الروابط
- [ ] مراجعة النصوص العربية والتأكد من صحتها
- [ ] اختبار سرعة تحميل الصور
- [ ] التأكد من عمل الأنيميشن بسلاسة

## 🚀 النشر

لنشر الموقع:

1. بناء المشروع:
   ```bash
   npm run build
   ```

2. رفع ملفات `dist` إلى الخادم

## 🔍 تحسين محركات البحث (SEO)

الموقع محسن لمحركات البحث مع:

### الملفات المضافة:
- `robots.txt` - توجيهات لمحركات البحث
- `sitemap.xml` - خريطة الموقع
- `manifest.json` - ملف التطبيق التقدمي (PWA)

### Meta Tags المضافة:
- وصف الموقع والكلمات المفتاحية العربية
- Open Graph للمشاركة على وسائل التواصل
- Twitter Cards للمشاركة على تويتر
- JSON-LD للبيانات المنظمة

### الأيقونات:
- Favicon من شعار الموقع
- أيقونات Apple Touch
- أيقونات PWA متعددة الأحجام

## 📞 الدعم

للحصول على المساعدة أو الإبلاغ عن مشاكل، تواصل مع فريق التطوير.

